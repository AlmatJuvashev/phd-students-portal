# ============================================
# KazNMU PhD Portal - Environment Variables
# ============================================

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/phd_portal?sslmode=disable

# JWT Configuration
JWT_SECRET=change-this-to-a-secure-random-string-in-production

# Server Configuration
PORT=8080
GIN_MODE=release

# CORS Configuration
# Frontend base URL (used for CORS validation)
# Important: use FRONTEND_BASE (not FRONTEND_BASE_URL)
FRONTEND_BASE=http://localhost:5173

# Redis Configuration (optional, for caching)
REDIS_URL=redis://localhost:6379

# Object storage (S3 / MinIO) for uploads
# Supply either S3_* variables or AWS_* fallbacks. When S3_ENDPOINT is empty the AWS SDK will use
# the default AWS S3 endpoint for the selected region.
S3_BUCKET=kaznmu-phd-portal-files
S3_REGION=us-east-1
# For MinIO/local deployments provide the explicit endpoint and enable path style
# S3_ENDPOINT=http://localhost:9000
S3_USE_PATH_STYLE=true
S3_ACCESS_KEY_ID=your-access-key
S3_SECRET_ACCESS_KEY=your-secret-key
# Legacy AWS_* names (optional fallback)
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_BUCKET_NAME=

# Email Configuration (optional, for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@kaznmu.edu.kz

# Optional: Unlock all nodes for testing
# VITE_UNLOCK_ALL_NODES=false

# ===========================================
# DEPLOYMENT-SPECIFIC (Railway + Vercel)
# ===========================================

# Railway Backend Environment Variables:
# - DATABASE_URL (auto-set by Railway Postgres plugin)
# - JWT_SECRET (generate secure random string)
# - PORT (default: 8280 or Railway's PORT)
# - GIN_MODE=release
# - FRONTEND_BASE (set to your Vercel URL, e.g., https://phd-students-portal.vercel.app)
# - S3 credentials if using file uploads
# - SMTP credentials if using email notifications

# Vercel Frontend Environment Variables:
# - VITE_API_URL (set to Railway backend URL + /api, e.g., https://your-backend.up.railway.app/api)
